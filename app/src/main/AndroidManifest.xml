<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.wiut.lineup.lineup">

    <application
        android:name=".dagger.inject.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".ui.test.TestActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="uz.wiut.lineup.lineup.ui.message.MessagesActivity" />
        <!--<activity android:name="uz.wiut.lineup.lineup.ui.my_profile.MyProfileActivity" />-->
        <activity android:name="uz.wiut.lineup.lineup.ui.home.OrganizationDetailsActivity" />
        <activity android:name="uz.wiut.lineup.lineup.ui.sign_up_in.SignInUpActivity"></activity>
    </application>

</manifest>